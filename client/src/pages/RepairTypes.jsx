import React from 'react';

export default function RepairTypes() {
  return (
    <>
      <p>
      // ask for the types of reapir that would be performed
      <br />
      // and in that case create a page for each service containing more about it.
      <br />
      // Maye question if we are getting an about page, or if we are putting everything in the home page.

      </p>
    </>
  );
}
